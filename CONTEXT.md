# 领域术语表

规范术语，不含实现细节。代码与界面文案都应与此保持一致。

## 身份档 (identity profile)

同一个人的一个独立分隔的凭据空间，各自持有自己的口令；解锁一个身份档时，看不到其他身份档的任何内容（包括条目名称与计数）。

- **不与「账号」混用**：`EntryType` 中的 `'account'` 指"网站与应用"类条目，与身份档无关。界面提到条目类型时沿用「网站与应用」，不得称身份档为账号。
- **不与「库」混用**：「密码库 / 保管库」指某个身份档当前加密的那份数据整体（磁盘上即 `vault.pvlt`）。一个身份档对应一份密码库，不反过来用「库」指代身份档。
- **地位平等**：所有身份档同属于本机使用者，没有主人档/子档之分，也没有跨身份档的管理者角色。

## 主密码 (master password)

解锁某个身份档所持有的口令。作用域是单个身份档，不同身份档之间互不相关。

## 条目 (entry)

密码库里保存的一条凭据，类型为「网站与应用 / 服务器 / API 凭据」之一。

## 默认身份档 (default profile)

磁盘上直接位于存储目录下的 `vault.pvlt` 所对应的那个身份档。它的显示名可由用户改（记在应用配置里，改的只是显示名，磁盘路径不变），并且始终存在——不能被删除。

## 新建身份档 (additional profile)

除默认身份档以外、以 `<存储目录>/<名字>/vault.pvlt` 形式共存的身份档。它的**名字即磁盘目录名，建档后不可改**：改名意味着移动磁盘对象，风险大于收益。要"换个名字"只能新建一个并把条目重新录一遍。

## 档案事实源 (vault-as-truth)

一个身份档是否存在，判据是它的 `vault.pvlt` 在不在，而**不是**任何配置里有没有它的记录。配置只用来存默认身份档的显示名，配置损坏不会让任何身份档的数据消失。

## 切换身份档 (switch profile)

从一个已解锁的身份档改去解锁另一个。全局同一时刻只有一个身份档处于解锁状态，切换隐含先锁定当前档。
